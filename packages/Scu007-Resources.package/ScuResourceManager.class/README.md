A ScuResourceManager is a renders and caches images.

Instance Variables
	cache:		Mapping from item symbol to rendered image form.
	imageSize:		Image resolution in pixels.
	images:		Mapping from image symbols to images.
